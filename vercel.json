{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": null,
  "buildCommand": "pnpm --filter @workspace/maa-sharda-market run build && node -e \"const fs=require('fs'); const src = fs.existsSync('artifacts/maa-sharda-market/dist') ? 'artifacts/maa-sharda-market/dist' : 'dist'; console.log('Copying build from: ' + src); if (src !== 'dist') { fs.cpSync(src, 'dist', {recursive:true}); } fs.cpSync(src, 'public', {recursive:true});\"",
  "outputDirectory": "dist",
  "installCommand": "pnpm install",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
